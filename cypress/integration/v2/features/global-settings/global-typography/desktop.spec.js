/**
 * External dependencies
 */
import { registerTests } from '~stackable-e2e/helpers'

/**
 * Internal dependencies
 */
import { desktopGlobalTypography } from './'

describe( 'Global Typography ( Desktop )', registerTests( [ desktopGlobalTypography ] ) )
