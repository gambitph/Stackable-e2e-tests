/**
 * External dependencies
 */
import { registerTests } from '~stackable-e2e/helpers'

/**
 * Internal dependencies
 */
import { desktopUnits } from './'

describe( 'Global Typography ( Desktop Units )', registerTests( [ desktopUnits ] ) )
