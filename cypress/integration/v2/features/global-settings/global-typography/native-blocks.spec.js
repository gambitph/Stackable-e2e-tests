/**
 * External dependencies
 */
import { registerTests } from '~stackable-e2e/helpers'

/**
 * Internal dependencies
 */
import { globalTypographyNativeBlocks } from './'

describe( 'Global Typography ( Native Blocks )', registerTests( [ globalTypographyNativeBlocks ] ) )
