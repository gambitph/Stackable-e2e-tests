/**
 * External dependencies
 */
import { registerTests } from '~stackable-e2e/helpers'

/**
 * Internal dependencies
 */
import { desktopBlock } from '.'

describe( 'Icon Label Block ( Desktop Block )', registerTests( [ desktopBlock ] ) )
