/**
 * External dependencies
 */
import { registerTests } from '~stackable-e2e/helpers'

/**
 * Internal dependencies
 */
import { mobileBlock } from '.'

describe( 'Icon Block ( Mobile Block )', registerTests( [ mobileBlock ] ) )
