/**
 * External dependencies
 */
import { registerTests } from '~stackable-e2e/helpers'

/**
 * Internal dependencies
 */
import { mobileStyle } from '.'

describe( 'Call to Action Block ( Mobile Style )', registerTests( [ mobileStyle ] ) )
