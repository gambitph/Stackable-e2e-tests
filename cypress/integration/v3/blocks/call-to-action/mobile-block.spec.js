/**
 * External dependencies
 */
import { registerTests } from '~stackable-e2e/helpers'

/**
 * Internal dependencies
 */
import { mobileBlock } from '.'

describe( 'Call to Action Block ( Mobile Block )', registerTests( [ mobileBlock ] ) )
