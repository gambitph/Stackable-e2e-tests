/**
 * External dependencies
 */
import { registerTests } from '~stackable-e2e/helpers'

/**
 * Internal dependencies
 */
import { desktopStyle } from '.'

describe( 'Call to Action Block ( Desktop Style )', registerTests( [ desktopStyle ] ) )
