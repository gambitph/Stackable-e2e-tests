/**
 * External dependencies
 */
import { registerTests } from '~stackable-e2e/helpers'

/**
 * Internal dependencies
 */
import { desktopBlock } from '.'

describe( 'Blockquote Block ( Desktop Block )', registerTests( [ desktopBlock ] ) )
